<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="../styles/styles.css">
  <title><%= folder.name %></title>
</head>

<body>

  <header>

    <%- include("partials/error.ejs") %>

    <a href="/">Go Back to Main</a>

    <% if (locals.currentUser) { %>
    <div>
      <h1><%= folder.name %></h1>
      <p>Created: <%= new Date(folder.createdAt).toLocaleString() %></p>
      <% } %>
    </div>

  </header>

  <main>

    <% if (locals.currentUser) { %>

    <h2>Upload File</h2>
    <form action="/uploadFile/<%= folder.id %>" method="post" enctype="multipart/form-data">
      <label for="files">Upload file: </label>
      <input type="file" id="files" name="files" multiple required />
      <button type="submit">Submit</button>
    </form>

    <% if (files.length > 0) { %>
    <h2>Files</h2>
    <ul class="folders">
      <% for (let i = 0; i < files.length; i++) { %>
      <li class="single-folder">

        <h3><%= files[i].name %></h3>

        <p>~<%= (files[i].size / 1000).toFixed(1) %>KB</p>

        <p>Uploaded: <%= new Date(files[i].uploadedAt).toLocaleString() %></p>

        <form action="/deleteFile/<%= folder.id %>/<%= files[i].id %>" method="post">
          <p><button type="submit" onclick="return confirm('Are you sure you want to delete this file?')">DELETE file</button></p>
        </form>

        <p><a href="/updateFile/<%= folder.id %>/<%= files[i].id %>">UPDATE file</a></p>

        <p><a href="/downloadFile/<%= folder.id %>/<%= files[i].id %>">Download/View file</a></p>

      </li>
      <% } %>
    </ul>
    <% } %>
    <% } %>
  </main>
</body>


</html>